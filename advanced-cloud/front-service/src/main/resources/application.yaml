spring:
  application:
    name: front-service
logging:
  level:
    root: INFO
server:
  port: 5555
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
      fetchRegistry: false